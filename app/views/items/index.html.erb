<div class="items_page" style="text-align: center;">
  <div class="title" style="border-bottom: 3px solid #004AAD;">
    <h1>Menu List</h1>
  </div>

<%= form_with url: cart_users_url(), local: true do |form| %>
    <div class="category">
      <div class="subtitle">
        <h2>Quick Meals</h2>
      </div>
      <div class="mt-5 menu">
        <% @meals.each do |item| %>
          <div class="row menuitem">
            <div class="col-md-3">
              <div class="card" style="width: 18rem;">
                <%= image_tag "meal.jpg" ,class: "card-img-top" %>
                <div class="card-body item_details">
                  <%= form.label item.name %>
                </div>
                <div class="quantity">
                  <%= form.label "$#{item.price}.0 x ", style: "color:black;" %>
                  <%= form.text_field item.id, class: "quantity-input m-2", size: "2x1" %>
                </div>
              </div>
            </div>
          </div>
        <% end %>
      </div>
    </div>

    <div class="category">
      <div class="subtitle">
        <h2>Snacks</h2>
      </div>
      <div class="mt-5 menu">
        <% @snacks.each do |item| %>
          <div class="row menuitem">
            <div class="col-md-3">
              <div class="card" style="width: 18rem;">
                <%= image_tag "snack.jpg" ,class: "card-img-top" %>
                <div class="card-body item_details">
                  <%= form.label item.name %>
                </div>
                <div class="quantity">
                  <%= form.label "$#{item.price}.0 x ", style: "color:black;" %>
                  <%= form.text_field item.id, class: "quantity-input m-2", size: "2x1" %>
                </div>
              </div>
            </div>
          </div>
        <% end %>
      </div>
    </div>

    <div class="category">
      <div class="subtitle">
        <h2>Drinks</h2>
      </div>
      <div class="mt-5 menu">
        <% @drinks.each do |item| %>
          <div class="row menuitem">
            <div class="col-md-3">
              <div class="card" style="width: 18rem;">
                <%= image_tag "drink.jpg" ,class: "card-img-top" %>
                <div class="card-body item_details">
                  <%= form.label item.name %>
                </div>
                <div class="quantity">
                  <%= form.label "$#{item.price}.0 x ", style: "color:black;" %>
                  <%= form.text_field item.id, class: "quantity-input m-2", size: "2x1" %>
                </div>
              </div>
            </div>
          </div>
        <% end %>
      </div>
    </div>
    <div class="m-5">
      <%= form.submit "View Cart", class: "btn btn-primary btn-officebreaks" %>
    </div>
<% end %>
</div>